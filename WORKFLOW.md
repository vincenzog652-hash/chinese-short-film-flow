# เวิร์กโฟลว์ผลิตหนังสั้นจีนแนวตั้ง

ใช้ ChatGPT หรือ Claude เป็นผู้ช่วยวางแผน และใช้ Google Flow สร้างสื่อ เป้าหมายคือหนังที่เล่าเรื่องต่อเนื่องและเสียงตรงบท ไม่ใช่แค่จำนวนช็อตที่เจนสำเร็จ

เวิร์กโฟลว์รองรับ 2 โหมด และใช้กติกา [Workflow V3](docs/WORKFLOW-V3.md) เพื่อควบคุมขอบเขต วางคอนเทนต์ และตรวจหลักฐานก่อนปิดงาน:

- **LEAN_SCOPE:** กำหนด must-have, nice-to-have, deliverable รอบนี้ และเพดาน take/เครดิต
- **CONTENT_PLAN:** วาง Hook, Conflict, Reveal, Cliffhanger, Caption และ CTA ก่อนผลิต
- **EVIDENCE_GATE:** ห้ามเลื่อนเป็น ACCEPTED, EXPORTED หรือ PUBLISHED โดยไม่มีหลักฐานขั้นต่ำ


- **Fiction:** เรื่องแต่ง เน้นตัวละคร บทสนทนา และความต่อเนื่องของฉาก
- **Documentary:** สารคดีหรือเรื่องจริง ต้องมี source ledger แยกข้อเท็จจริง การตีความ และภาพจำลอง ดู [คู่มือโหมดสารคดี](docs/DOCUMENTARY.md)

## 1. ล็อกบรีฟ

กรอก [PRODUCTION.md](templates/PRODUCTION.md): แนวเรื่อง ยุค ผู้ชม อัตราส่วน ความยาว ภาษา จำนวนตัวละคร งบเครดิต และขอบเขตการทำงาน

ค่าเริ่มต้นเมื่อไม่มีข้อกำหนด: จีนย้อนยุคสมมติ ตัวละครผู้ใหญ่ 2 คน ภาษาไทย 9:16 ตอนนำร่อง 48 วินาที แบ่ง 6 ช็อต ความยาวนี้เป็นตัวอย่างของ repo ไม่ใช่ข้อบังคับของ Google

ก่อนเขียนบท ให้ทำ duration plan จากความยาวตัดต่อเป้าหมายและค่าที่โมเดลรองรับจริง เช่น 30, 45 หรือ 60 วินาทีอาจใช้ช็อตยาวไม่เท่ากัน ห้ามคำนวณจำนวนช็อตจากสมมติฐาน 8 วินาทีโดยไม่ตรวจ UI ระบุเวลาสามค่าแยกกันเสมอ: `target edit duration`, `supported generation duration` และ `actual exported duration`

เลือก hook → เหตุขัดแย้ง → หลักฐานใหม่ → cliffhanger ให้ทุกช็อตขยับเรื่อง หลีกเลี่ยงบทพูดยาวที่ใส่ไม่ทัน จากนั้นร่าง Caption/CTA ให้สอดคล้องกับสิ่งที่จะเห็นจริง แต่ยังไม่อนุมัติเป็นข้อความเผยแพร่จนกว่าไฟล์ final จะผ่าน QC

**ผ่านเมื่อ:** รู้ว่าใครต้องการอะไร มีอุปสรรคอะไร และตอนนี้จบตรงไหน บันทึกสมมติฐานที่ผู้ใช้ยังไม่ได้ระบุ

สำหรับ Documentary ต้องผ่านเพิ่มเมื่อข้อกล่าวอ้างสำคัญทุกข้อมีแหล่งอ้างอิงหรือถูกทำเครื่องหมายว่ายังไม่ยืนยัน และกำหนดป้าย `ภาพจำลอง` สำหรับภาพที่อาจถูกเข้าใจว่าเป็นเหตุการณ์จริง

## 2. ล็อกตัวละครและความต่อเนื่อง

สร้าง character bible ระบุ ID อายุผู้ใหญ่ ใบหน้า ทรงผม ชุด สี เครื่องประดับ เสียง วิธีพูด และสิ่งที่ห้ามเปลี่ยน ให้แต่ละคนมีภาพอ้างอิงเดี่ยวแยกไฟล์

ระบุฉาก เวลา แสง ทิศทางสายตา ตำแหน่งซ้าย/ขวา และสถานะอุปกรณ์ก่อน–หลังช็อต ตัวอย่าง: ตราหยกอยู่มือขวา L01 ตลอดจนถึงช็อตส่งมอบจริง

**ผ่านเมื่อ:** ชื่อและรายละเอียดในบท ภาพ และพรอมป์ต์ตรงกันทั้งหมด เมื่อผู้ใช้เลือกเวอร์ชันแล้วเก็บเวอร์ชันนั้นเป็นต้นฉบับอ้างอิง

## 3. บทและ storyboard

หนึ่งช็อตมีหนึ่งเหตุการณ์หลัก ให้หนึ่งคนพูดต่อช็อตเป็นค่าเริ่มต้น อีกคนปิดปากและแสดงปฏิกิริยา ถ้าต้องพูดโต้ตอบให้แยกช็อตก่อน

แต่ละช็อตต้องมี ID, เวลาตัดต่อ, ผู้พูด, บทตรงตัว, ภาพเริ่มต้น, การเคลื่อนไหว, กล้อง, เสียง, ภาพอ้างอิง และเกณฑ์ผ่าน เว้นช่วงหายใจและปฏิกิริยา อ่านบทออกเสียงเพื่อเช็กเวลา

**ผ่านเมื่อ:** เวลารวมตรงเป้าหมาย ทุกประโยคมีเจ้าของบท ไม่มีตัวละครหรือสถานที่โผล่มาโดยไม่มีการเตรียม

## 4. ภาพอ้างอิงและเฟรมเริ่มต้น

ใช้พรอมป์ต์ภาพจาก character bible สร้างภาพเดี่ยว ตรวจหน้า ชุด สี มือ และอุปกรณ์ก่อนสร้างวิดีโอ หากสร้างภาพใน ChatGPT ไม่ได้ หรือใช้ Claude แบบแชต ให้ใช้ Google Flow สร้างภาพแทน

ภาพรวมตัวละครหลายช่องใช้ดูแบบได้ แต่ควรเลือกภาพเดี่ยวสะอาดเป็น reference ของแต่ละคน ทำเฟรมเปิดแนวตั้งที่มีตัวละครและฉากตามช็อต ไม่เปลี่ยนรูปลักษณ์เพื่อให้พรอมป์ต์ใหม่ดูสวยขึ้น

**ผ่านเมื่อ:** ภาพอ้างอิงแต่ละไฟล์มี ID ตรงกับตัวละคร และผู้ใช้เลือกภาพที่จะใช้แล้ว

## 5. ทดสอบหนึ่งช็อตก่อน

ตรวจโหมด โมเดล 9:16 ระยะเวลา จำนวนผลลัพธ์ และเครดิตใน UI จริง เลือก Frames หรือ Ingredients ตามที่บัญชีรองรับ ใช้ [GOOGLE-FLOW.md](docs/GOOGLE-FLOW.md)

เริ่มช็อตหนึ่งก่อน หากมีสิทธิ์ใช้เครดิตอยู่แล้วทำตามขอบเขตนั้น หากยังไม่มีงบหรือคำสั่งให้เจน ให้ส่งพรอมป์ต์พร้อมประเมินจำนวนครั้งและรอข้อมูลที่ขาด

**ผ่านเมื่อ:** เปิดผลลัพธ์จริงได้ ภาพเหมือน reference ได้ยินผู้พูดที่ถูกคน และบทไม่เพิ่มหรือหาย การแสดงสถานะ completed อย่างเดียวไม่พอ

ก่อนกดสร้าง ให้บันทึกเพดานงบทั้งตอน ต้นทุนที่ UI แสดงต่อผลลัพธ์ และจำนวน take สูงสุด คำนวณ `estimated maximum = cost per result × outputs per attempt × allowed attempts` หากค่าใดไม่ทราบ ให้หยุดที่ชุดพรอมป์ต์และระบุข้อมูลที่ต้องตรวจ ห้ามประมาณว่าเครดิตฟรีหรือเครดิตจะคืนเมื่อ error

## 6. สร้างที่เหลือและแก้เฉพาะจุด

เก็บช็อตที่ผ่านแล้ว สร้างช็อตถัดไปด้วย reference เดิม เก็บชื่อเช่น `EP01_S02_take01.mp4` และ prompt เวอร์ชันที่ใช้

ค่าเริ่มต้นการทดลองที่ repo แนะนำ: รวมไม่เกิน 3 ครั้งต่อช็อต หรือจนงบที่อนุญาตหมด แล้วแต่ถึงก่อน ตรวจความผิดพลาดก่อนทุกครั้ง ไม่เรียกซ้ำขณะงานเดิมยังรันอยู่ เปลี่ยนตัวแปรครั้งละหนึ่งอย่าง เช่น ลดการกระทำ หรือเปลี่ยนโมเดลที่รองรับโหมดเดียวกัน

ทุก retry ต้องมีสมมติฐานว่ากำลังแก้อะไร ตัวแปรที่เปลี่ยน และผลที่เห็นจริง หากสองครั้งติดกันไม่ดีขึ้นด้วยสาเหตุเดิม ให้เปลี่ยนกลยุทธ์ เช่น แยกช็อต ใช้เฟรมเริ่มต้นใหม่ หรือนำเสียงไปพากย์แยก แทนการวน prompt เดิม

หากไม่รู้สาเหตุ error ให้บันทึกว่าไม่ทราบ ห้ามสรุปว่าเซิร์ฟเวอร์ล่มหรือเครดิตคืนแล้วโดยไม่มีหลักฐาน ใช้ [REPAIR.md](prompts/REPAIR.md)

**ผ่านเมื่อ:** ทุกช็อตมีไฟล์ที่ตรวจแล้ว พร้อมบันทึกปัญหาและ take ที่เลือก

## 7. ตัดต่อและตรวจไฟล์ส่งมอบ

เรียงและตัดคลิปใน Flow Scenebuilder หากบัญชีมีเมนูนี้ หรือใช้โปรแกรมตัดต่อที่มีอยู่แล้ว ตัดช่วงเงียบให้ได้จังหวะและความยาวเป้าหมาย ไม่ยืดบทพูดเพื่อให้ครบ 48 วินาที

เพิ่มซับไทยจากบทที่ตรวจแล้วในขั้นตัดต่อ ตรวจไม่ให้บังใบหน้าและส่วนสำคัญบนมือถือ จัดดนตรีให้ไม่กลบคำพูด หากเสียงเจนผิดซ้ำ ให้ทำเสียงพากย์ที่ได้รับอนุญาตแยกและปิดเสียงพูดเดิม พร้อมตรวจ lip sync อีกครั้ง

- [ ] ไฟล์เปิดเล่นได้ตั้งแต่ต้นจนจบ และเป็น 9:16
- [ ] หน้า ชุด อุปกรณ์ แสง และตำแหน่งต่อเนื่องทุกจุดตัด
- [ ] ฟังทุกประโยคเทียบกับบท ตรวจผู้พูด คำตก คำเพิ่ม ภาษา และเสียงซ้ำ
- [ ] ซับตรงเสียงและอ่านทัน ไม่มีข้อความผิดจากการเจนติดภาพ
- [ ] ไม่มีเฟรมดำผิดที่ เสียงแตก หรือดนตรีกลบเสียง
- [ ] ความยาวจริงและความละเอียดถูกบันทึก หากเป็น 720p ให้ระบุจริง ไม่เรียก 1080p
- [ ] ชื่อไฟล์ final แตกต่างจาก take และ draft
- [ ] เครดิตที่ใช้จริง จำนวน retry และ take ที่เลือกตรงกับ production log
- [ ] Documentary: แหล่งอ้างอิงและป้ายภาพจำลองครบ ไม่มีภาพ AI ถูกนำเสนอเป็นหลักฐานจริง

หากไม่มีเครื่องมือฟัง/ดู ให้ระบุ `NEEDS_HUMAN_REVIEW` ไม่ทำเครื่องหมายว่าตรวจผ่านแทนผู้ใช้

## 8. แพ็กส่งมอบและเผยแพร่

ส่งไฟล์ final, บท, reference, prompt ที่ใช้, take ที่เลือก, QC log และแคปชั่นร่าง เมื่อได้รับคำสั่งเผยแพร่ ให้ตรวจบัญชีและไฟล์ที่เลือกก่อนโพสต์ ตรวจรายการที่มีอยู่เพื่อป้องกันโพสต์ซ้ำ และเปิดโพสต์จริงเพื่อยืนยัน URL หลังทำเสร็จ

สถานะที่ใช้: `PLANNED → READY → GENERATING → REVIEW → ACCEPTED` หรือ `RETRY / BLOCKED` ส่วน `EXPORTED` และ `PUBLISHED` เป็นสถานะระดับตอน ไม่ปะปนกับช็อต ไม่มีการเลื่อนสถานะเองเพียงเพราะเขียน prompt เสร็จ ทุกสถานะปลายทางต้องผ่าน `EVIDENCE_GATE` และมีชื่อไฟล์, QC หรือ URL ตามที่กำหนดใน Workflow V3
